/* Ejercicio pendiente. Repasar metodos de arrays. */
let numeros = [10, 20, 30, 40, 50];
let promedio;

function calcularPromedio(numeros){
    let suma = numeros.reduce()
};
console.log("Promedio:", promedio);